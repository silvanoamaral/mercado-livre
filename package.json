{
  "name": "mercado-livre",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.11.0",
    "query-string": "^6.4.2",
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.5",
    "serve": "^10.1.2"
  },
  "devDependencies": {
    "enzyme": "^3.9.0",
    "enzyme-adapter-react-16": "^1.12.1"
  },
  "scripts": {
    "dev": "PORT=8000 react-scripts start --mode development --open --inline --hot",
    "start": "serve -s build",
    "build": "react-scripts build",
    "test": "react-scripts test --coverage --watch --colors",
    "test:jest": "jest --updateSnapshot --coverage --watch",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
